spring:
  application:
    name: server
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_and_angular_practice
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
